name: Bug report
description: Report incorrect behavior, a crash, data loss, or a coherence violation
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for filing a bug. For suspected security vulnerabilities, do NOT
        open a public issue — follow [SECURITY.md](https://github.com/steerlabs/portablefs/blob/main/SECURITY.md).
  - type: textarea
    id: what-happened
    attributes:
      label: What happened
      description: What did you observe, and what did you expect instead?
      placeholder: A clear description of the bug and the expected behavior.
    validations:
      required: true
  - type: textarea
    id: repro
    attributes:
      label: Reproduction
      description: Minimal steps (commands, mount ops, workload) to reproduce.
      placeholder: |
        1. portablefs mount ...
        2. ...
        3. observed ...
    validations:
      required: true
  - type: dropdown
    id: component
    attributes:
      label: Component
      options:
        - Authority server (direct store / XFS / WAL)
        - Mount client (Linux FUSE)
        - Mount client (macOS FSKit / portablefsd)
        - CLI
        - Installer / release artifacts
        - Docs
        - Other / not sure
    validations:
      required: true
  - type: textarea
    id: environment
    attributes:
      label: Environment
      description: OS + arch, mount transport, how the authority is deployed (flags, TLS, storage), and version / commit.
      placeholder: |
        - OS/arch: macOS 15 arm64 / Ubuntu 24.04 amd64
        - Transport: FSKit / FUSE
        - Authority: local binary / self-hosted (XFS backing store? TLS?)
        - Version or commit:
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: Logs
      description: Relevant logs (redact secrets). Include the x-portablefs-request-id if you have one.
      render: text
