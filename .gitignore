node_modules
dist
.turbo
.volume-cache
.env
.env.*
!.env.example
!.env.*.example
.env.local
.railway
coverage
*.tsbuildinfo

# Secrets and private keys — never commit these (defense-in-depth; real config
# flows through env / .env.* which are already ignored above)
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_ed25519
*_rsa
*_ed25519
credentials.json
*.credentials.json

# Go build artifacts
vcs-bin
nfsio-bin
mount-bin
fsio-bin
vcs/mount
vcs/spike
vcs/treehashcheck
vcs/portablefs-authority
*.wal
linux-bins/
*-linux-amd64
*.test

# Python/kernel-verifier and checkpatch scratch artifacts
__pycache__/
*.py[cod]
.checkpatch-camelcase.git.

# Additional local build outputs, coverage files, and OS/IDE cruft
vcs/vcs
portablefsd-bin
.DS_Store
**/.DS_Store
xcuserdata/
**/xcuserdata/
DerivedData/
/.build/
/.swiftpm/
cover.out
coverage.out
*.coverprofile

# Local qualification receipts can contain mount credentials, machine paths,
# and historical logs. They are evidence, not source or release artifacts.
.qualification/
